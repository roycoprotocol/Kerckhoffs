# Roles

| File | Contents |
|---|---|
| [`assignments.md`](./assignments.md) | Who holds each role — per-role table with admin / guardian / holder / delay. The canonical "what's the security model" doc. |
| [`auth.md`](./auth.md) | What each role can do — per-role exhaustive list of every (target, selector) pair gated by it. |
| [`diagram.md`](./diagram.md) | Mermaid diagrams: actors → roles, roles → contracts, cross-org dependencies. |

Every change to the on-chain role topology should land in all three files in the same PR.

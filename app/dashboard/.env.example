# Per-chain subgraph GraphQL endpoints (Goldsky in prod; local graph-node in dev).
# The dashboard reads these server-side (Server Components) — no NEXT_PUBLIC prefix.
SUBGRAPH_URL_1=http://localhost:8000/subgraphs/name/royco-access-mainnet
SUBGRAPH_URL_43114=
SUBGRAPH_URL_42161=
SUBGRAPH_URL_8453=

# Per-chain RPC endpoints (used by viem for Makina governance-slot reads, mainnet only today).
RPC_URL_1=
RPC_URL_43114=
RPC_URL_42161=
RPC_URL_8453=
